# Organization profile repository

This directory is a separate Git checkout for the GitHub organization profile,
not CI configuration for the parent workspace. Its public content lives in
`profile/README.md`. No dependency installation or build is required.

Verify product claims against current source; older descriptions of the
reference UI as having no backend are historical. Preserve organization-relative
GitHub links and bilingual naming. Preview Markdown and run `git diff --check`.
Use this repository's Git history and inspect existing changes before editing.
Do not rename the remote `.github` repository or publish changes unless requested.
